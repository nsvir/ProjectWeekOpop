<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:projectWeek="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/default_search"
        android:icon="@android:drawable/ic_menu_search"
        android:title="search"
        projectWeek:showAsAction="always"
        projectWeek:actionViewClass="android.support.v7.widget.SearchView"
        />

    <item
        android:id="@+id/logout"
        android:title="@string/logout"
        android:orderInCategory="50"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_settings"
        android:title="@string/action_settings"
        android:orderInCategory="100"
        app:showAsAction="never" />
    <!-- other items or whatever  -->

</menu>